const SectionTitle = ({ text }) => {
  return (
    <div className="border-b border-base-300 pb-5">
      <h2 className="font-3xl tracking-wider font-medium capitalize">{text}</h2>
    </div>
  );
};

export default SectionTitle;
